/* 
Sắp xếp tăng dần 
Array.sort()
Sắp xếp giảm dần
Array.sort().reverse()
*/

var arr = [1,5,5,8,0,2,4,7]
console.log(arr.sort()) //Sắp xếp tăng dần
console.log(arr.sort().reverse()) //Sắp xếp giảm dần
