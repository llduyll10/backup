// Array.shift()
// Method removes the first element from an aray and returns that removed element.
// This method changes the length of the array

const array1 = [1, 2, 3]
const firstElement = array1.shift()
console.log(array1);
console.log(firstElement);