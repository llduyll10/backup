// Array.push()
// Adds one or more elements to the end of an array and returns the new length of the array


const animals = ['pigs', 'goats', 'sheep'];
const count = animals.push('cows')
console.log(count); // show output = 4
console.log(animals);