//Array.join()
// Methods creates and returns a new string by concatenating all of the elements in an array
//, seperated by commas or a specified separator string
const elements = ['Fire', 'Air', 'Water'];

console.log(elements.join());

console.log(elements.join(''));

console.log(elements.join('-'));